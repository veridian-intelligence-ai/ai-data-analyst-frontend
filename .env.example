# Base URL of the AI Data Analyst backend (the FastAPI starter service).
# REQUIRED. The app refuses to boot without it — see src/lib/config.ts for why
# a silent fallback URL is deliberately not provided.
# Local development:
VITE_API_BASE_URL=http://localhost:8000
